<nav class="navbar navbar-expand-lg navbar-dark fixed-top shadow navbar-custom">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center gap-2" href="/">
            <span class="fw-bold">Mercado Business</span>
        </a>

        <!-- Botão Mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Conteúdo do Menu -->
        <div class="collapse navbar-collapse" id="menu">
            <% if (isAuthenticated) { %>
                <!-- Barra de busca para desktop (visível apenas em lg) -->
                <form class="d-none d-lg-flex mx-auto search-form" role="search" action="/users" method="GET" style="min-width: 300px;">
                    <input class="form-control me-2" type="search" name="q" placeholder="Pesquisar..." aria-label="Search" value="<%= searchQuery || '' %>">
                    <button class="btn btn-light" type="submit">Buscar</button>
                </form>

                <!-- Barra de busca para mobile (visível apenas no menu collapse) -->
                <form class="d-lg-none my-3" role="search" action="/users" method="GET">
                        <div class="input-group">
                            <input type="search" class="form-control" name="q" placeholder="Pesquisar usuários..." aria-label="Search" value="<%= searchQuery || '' %>">
                            <button class="btn btn-light" type="submit">Buscar</button>
                        </div>
                </form>

                <ul class="navbar-nav ms-auto align-items-center gap-2">
                    <li class="nav-item">
                        <a href="/" class="nav-link fw-bold <%= currentUrl === '/' ? 'active' : '' %> ">Home</a>
                    </li>
                    <li class="nav-item">
                        <% if (isMyUser) { %>
                            <a href="/profile/user/<%= myUser.id %>" class="nav-link fw-bold active">Meu Perfil</a>
                        <% } else { %>
                            <a href="/profile/user/<%= myUser.id %>" class="nav-link fw-bold">Meu Perfil</a>
                        <% } %>
                    </li>
                    <li class="nav-item">
                        <a href="/auth/logout" class="btn btn-light fw-bold" style="color: #2a5298;">Sair</a>
                    </li>
                </ul>
            <% } else { %>
                <ul class="navbar-nav ms-auto align-items-center gap-2">
                    <li class="nav-item">
                        <a href="/auth/login" class="btn btn-light fw-bold" style="color: #2a5298;">Login</a>
                    </li>
                </ul>
            <% } %>
        </div>
    </div>
</nav>